import React from 'react'
import { staffsinfo } from '../data'
import CardModal from './CardModal'

const ModalStaff = (props) => {
    const { match:{ params: {userid}}} = props


    return (
        <div>
            
        </div>
    )
}

export default ModalStaff
